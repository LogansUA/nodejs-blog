ul
    li
        a(href='/') Home page
    if user
        li= user.email
        li
            ul
                if user.role == 'admin'
                    li
                        a(href='/admin/dashboard') Dashboard
                li
                    a(href='/user/logout') Logout
    else
        li
            a(href='/user/login') Login
